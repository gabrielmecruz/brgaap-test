<mvc:View
  controllerName="brgaap-test.controller.Detail"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc">
  <Page 
    title="Detalhes da nota fiscal #{invoice>id}"
    showNavButton="true"
    navButtonPress=".onBack">
    <Panel id="detail-panel">
    <ObjectHeader
      fullScreenOptimized="true"
      responsive="true"
      title="Nota Fiscal #{invoice>id}"
      intro="Nome: {invoice>title}">
        <ObjectAttribute title="User ID" text="{invoice>userId}"/>
        <ObjectAttribute title="Completado" text="{= ${invoice>completed} === true ? 'Sim' : 'Não' }"/>
    </ObjectHeader>
    </Panel>
  </Page>
</mvc:View>